//Xây dựng lớp đối tượng Prototype (giống như class). Chúng ta nên đặt tên prototype giống với tên file để dễ nhớ và liên tưởng
function SinhVien(masv,hoten,email,sodt,cmnd)
{
    this.MaSV = masv;
    this.HoTen = hoten;
    this.Email = email;
    this.SoDT = sodt;
    this.CMND = cmnd;
}